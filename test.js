function concatStrings(strA, strB) {
  return strA + strB;
}
concatStrings("abc", "def") // returns "abcdef"

concatStrings(1,2) // returns 12 which is the wrong response for the function since it was numbers that was passed in